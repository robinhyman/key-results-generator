{
  "name": "key-results-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "setup": "git config core.hooksPath .githooks",
    "prepare": "git config core.hooksPath .githooks || true",
    "check": "node ai-team/bin/increment-check.mjs --mode=push",
    "test": "node --test",
    "test:browser": "playwright test",
    "lint": "node --check server.js && node --check src/generator.js && node --check src/ai-service.js && node --check src/ai/client.js && node --check src/ai/constants.js && node --check src/ai/errors.js && node --check src/ai/format.js && node --check src/ai/normalize.js && node --check src/ai/prompts.js && node --check src/ai/schemas.js && node --check src/ai/tracing.js && node --check public/api.js && node --check public/app.js && node --check public/format.js && node --check public/render.js",
    "build": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
